// src/components/FilterPanel.jsx
import React from 'react';

const FilterPanel = () => {
  return (
    <div>
      {/* Filtros por fechas, habitación, estado, etc */}
      <p>Panel de Filtros</p>
    </div>
  );
};

export default FilterPanel;
